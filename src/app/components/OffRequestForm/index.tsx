/**
 *
 * OffRequestForm
 *
 */
import React, { memo } from 'react';
import styled from 'styled-components/macro';

interface Props {}

export const OffRequestForm = memo((props: Props) => {
  return <Div></Div>;
});

const Div = styled.div``;
