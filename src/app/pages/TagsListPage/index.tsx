/**
 *
 * TagsListPage
 *
 */
import React, { memo } from 'react';
import styled from 'styled-components/macro';

interface Props {}

export const TagsListPage = memo((props: Props) => {
  return <Div>Tags List Page</Div>;
});

const Div = styled.div``;
